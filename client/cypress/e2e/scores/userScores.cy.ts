describe("User scores", () => {
  it("should display the user scores page", () => {});

  it("should display the user scores for the different modes", () => {});

  it("should display the user scores for the different countries", () => {});

  it("should be able to search a country", () => {});
});
