describe("Country scores", () => {
  it("should go the country scores from the country modal", () => {});

  it("should display the global country scores", () => {});

  it("should display the best scores", () => {});

  it("should display the user best scores", () => {});

  it("should display the best scores for a specific mode", () => {});

  test("when the user want to see the top scores users", () => {
    it("should go to the user profile if the user exists", () => {});

    it("should not be able to go to the user profile if the user does not exist", () => {});
  });
});
