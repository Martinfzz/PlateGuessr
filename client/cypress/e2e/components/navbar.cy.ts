describe("Navbar", () => {
  test("when the logo props is set to true", () => {
    it("should display the logo", () => {});
  });
  test("when the logo props is set to false", () => {
    it("should not display the logo", () => {});
  });
  test("when the user is not connected", () => {
    it("should display the login button", () => {});
    it("should display the signup button", () => {});
  });
  test("when the user is connected", () => {
    it("should display the user button", () => {});
  });
  test("when the user is connected and the user button is clicked", () => {
    it("should display the user menu", () => {});
    it("should display the account link", () => {});
    it("should display the stats link", () => {});
    it("should display the logout link", () => {});
  });
  test("when the go back button is clicked", () => {
    it("should go back to the main page", () => {});
  });
  test("when the user click on the theme toggle button", () => {
    it("should change the theme", () => {});
  });
  test("when the user click on the language selector", () => {
    it("should change the language", () => {});
  });
  test("when the page props is set", () => {
    it("should display the page badge", () => {});
  });
  test("when the page props is not set", () => {
    it("should not display the page badge", () => {});
  });
});
